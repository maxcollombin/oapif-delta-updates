files:
  - path: ./data/obs.geojson
    # The ../_doc/$DOCUMENT_ID endpoint cannot be retrieve when property param is chosen as id_field. It looks like random (?) id are generated by Elasticsearch 
    # id_field: geonameid
    id_field: id
